# dict = {
#     'Asia':'red',
#     'Europe':'green',
#     'Africa':'blue',
#     'Americas':'yellow',
#     'Oceania':'black'
# }

# plt.scatter(x = gdp_cap, y = life_exp, 
# s = np.array(pop) * 2, c = col, alpha=0.8) # alpha is for opacity.
# c takes a list. alpha is b/w 0 and 1 where 0 is all transparent and
# 1 is not at all transparent.

# plt.text(1550, 71, 'India') # To add grid lines text.
# plt.text(5700, 80, 'China')

# plt.grid(True) so that grid lines are drawn on the plot.